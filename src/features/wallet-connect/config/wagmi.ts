/**
 * Wagmi Configuration Re-export
 *
 * @deprecated Import from '@/shared/config' instead
 *
 * This file re-exports the Wagmi config from shared for backward compatibility.
 * All new code should import directly from shared.
 */

export { wagmiConfig, wagmiConfig as config, chains } from '@/shared/config'
