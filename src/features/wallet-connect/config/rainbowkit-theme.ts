/**
 * RainbowKit Theme Configuration - Re-export from shared
 *
 * Theme implementation moved to shared/config for FSD compliance.
 * This file re-exports for backwards compatibility with existing imports.
 *
 * @see @/shared/config/rainbowkit-theme for implementation
 */

export {
  voidPayTheme,
  VOIDPAY_ACCENT_COLOR,
  VOIDPAY_COLORS,
  createVoidPayTheme,
} from '@/shared/config/rainbowkit-theme'
