# Part 4: Web3 Mechanics & Interaction

> **Status:** Brainstorm Result
> **Date:** November 18, 2025
> **Topic:** Wallet Strategy, Validation & Verification Logic

## 4.1. Стратегия подключения кошелька (Wallet Strategy)

Мы придерживаемся принципа **Progressive Disclosure** (Постепенное раскрытие). Мы не требуем подключения кошелька, пока это не станет абсолютно необходимым.

### 4.1.1. Для Создателя (Creator)

- **Режим:** Опциональный.
- **Польза:**
  - Автозаполнение адреса получателя.
  - Подтягивание ENS имени (`alex.eth`) и аватара для повышения доверия.
- **UX:** Кнопка "Connect" маленькая и находится рядом с полем "Wallet Address". Если не подключен — можно ввести адрес вручную.

### 4.1.2. Для Плательщика (Payer)

- **Режим:** Read-only по умолчанию -> Обязательный для оплаты.
- **Сценарий:**
  1.  При открытии ссылки приложение работает в режиме "Только чтение" (как обычный Web2 сайт). Бухгалтер может скачать PDF без Метамаска.
  2.  При нажатии кнопки "Pay" инициируется подключение через **RainbowKit**.
- **Wallet UI:** **RainbowKit v2**
  - Красивый готовый modal для выбора кошельков.
  - Поддержка EIP-6963 (множественные injected providers).
  - Автоматическое определение Metamask, Rainbow, Coinbase Wallet, WalletConnect.
  - Легкая кастомизация под brand (violet accent).
- **Интеграция:** RainbowKit обернут вокруг Wagmi v2, обеспечивая seamless UX.

## 4.2. Логика Валидации (Validation Logic)

Поскольку мы работаем с деньгами, фронтенд должен параноидально проверять данные перед формированием транзакции.

### 4.2.1. Проверка токенов (Decimals & Security)

- **Decimals Strategy (Вариант А - Snapshot / Baked-in):**
  - **При создании инвойса (Editor):** Создатель инвойса делает **один RPC-запрос** `readContract(tokenAddress, 'decimals')` к ноде и узнает значение decimals токена.
  - **Запекание в URL:** Полученное значение "запекается" прямо в параметры URL: `&dec=6` (для USDC), `&dec=18` (для ETH/WETH).
  - **При оплате:** Страница `/pay` мгновенно загружается **без дополнительных RPC-вызовов** — все необходимые данные (decimals, сумма, адреса) уже закодированы в URL.
  - **Преимущества:**
    - ⚡ Мгновенная загрузка страницы оплаты (нет задержки на RPC).
    - 🔒 Снижение нагрузки на RPC провайдера.
    - 🎯 Детерминированное поведение (decimals зафиксирован при создании).
- **Anti-Scam (Token Lists):**
  - **Blue Chips List:** По умолчанию предлагается список проверенных токенов (USDC, USDT, DAI, WETH) из Uniswap Token List.
  - **Manual Entry:** Возможность ручного ввода адреса токена для кастомных токенов.
  - _Status Verified:_ Зеленая галочка для токенов из Blue Chips списка.
  - _Status Unknown:_ Желтый ворнинг "Unknown Token. Verify contract address carefully" для токенов вне списка.

### 4.2.2. Защита от Reorgs (Confirmation Waiting)

Чтобы избежать ситуации, когда фронтенд показал "Paid", а транзакция откатилась при реорганизации цепочки:

- **Стратегия:** Ждем статус **`finalized`** для всех сетей.
  - _Ethereum Mainnet:_ ~15 минут (2 epochs).
  - _Arbitrum/Optimism:_ ~10-15 минут (зависит от L1 финализации).
  - _Polygon PoS:_ ~30-45 минут (из-за более высокого риска reorg).
- **UI Статусы:**
  1. **"Processing..."** — транзакция отправлена, ждем включения в блок.
  2. **"Confirming..."** — транзакция в блоке, ждем финализации.
  3. **"Paid ✓"** — транзакция финализирована, безопасно считать оплаченной.
- **Обоснование:** Максимальная безопасность для получателя. Плательщик видит промежуточные статусы и понимает процесс.

## 4.3. Логика Верификации (Verification / Status Check)

Как приложение понимает, что инвойс оплачен, без бэкенда?

### 4.3.1. Метод "Magic Dust Verification" (Магическая пыль)

При загрузке страницы оплаты (и периодически каждые **10 секунд**) фронтенд опрашивает блокчейн. Вместо "Fuzzy Matching" (проверка с погрешностью), используется метод **"Magic Dust"** для уникальной идентификации платежа.

**Концепция:**
К запрашиваемой сумме добавляется случайный микро-хвост (например, `+0.000042 USDC`), который делает каждую транзакцию уникальной и легко идентифицируемой. Это решает проблему множественных платежей одной суммы одному получателю.

**Алгоритм генерации Magic Dust (при создании инвойса):**

1.  **Генерация случайного числа:** Создается криптографически безопасное случайное число в диапазоне `0.000001` - `0.000999` (для токенов с 6 decimals как USDC) или `0.000000000000000001` - `0.000000000000000999` (для токенов с 18 decimals как ETH).
2.  **Добавление к сумме:** Случайное число добавляется к основной сумме инвойса.
    - _Пример:_ Инвойс на 1000 USDC → Финальная сумма `1000.000042 USDC`.
3.  **Сохранение в URL:** Точная сумма с Magic Dust сохраняется в поле `amount` (или кодируется в line items).
4.  **UI Transparency (Вариант А - Pretty Print):**
    - В Hero Section показывается **Pretty Print**: `1,000.00 USDC` (красиво округленная сумма, крупным шрифтом).
    - Мелким шрифтом ниже: **Точная сумма** `"Payment amount: 1,000.000042"` для прозрачности.
    - На кнопке оплаты: **Pretty Print** `"Pay 1,000.00 USDC"` (точная сумма уже видна в мелком тексте выше).

**Алгоритм верификации (на странице оплаты):**

1.  **Fetch:** Запрашиваем историю входящих транзакций на кошелек получателя.
    - _Источник:_ **Alchemy Transfers API**. Интегрируется с RPC провайдером, быстрее чем прямой `getLogs`.
    - _Endpoint:_ `alchemy_getAssetTransfers` с фильтром `toAddress`.
2.  **Filter:** Ищем транзакцию с **точным соответствием**:
    - `to` == Адрес получателя из инвойса (`f.w`).
    - `token` == Адрес токена из инвойса (`t`).
    - `value` === **Точная ожидаемая сумма** (с Magic Dust, без погрешности).
      - _Формула:_ `receivedAmount === expectedAmountWithDust`
      - Нет допуска погрешности — Magic Dust обеспечивает уникальность.
    - `timestamp` >= Дата создания инвойса (`iss` field).
    - `from` == Адрес плательщика (Опционально, если указан в `c.w`).
3.  **Result:**
    - Если найдено **точное совпадение** -> UI переходит в состояние **"Confirming..."** (ждем финализации).
    - Если не найдено -> статус остается **"Pending"** (продолжаем polling).
    - После финализации -> статус **"Paid ✓"**.
    - **TxHash Discovery:**
      - **Payer:** Stores the `txHash` in `usePayerStore` (LocalStorage) immediately after signing.
      - **Creator:** Discovers the `txHash` via Alchemy Transfers API polling (matching the Magic Dust amount) and stores it in `useCreatorStore` (LocalStorage).
      - **Note:** The `txHash` is **NOT** stored in the Invoice URL to keep the URL static and shareable.

**Преимущества Magic Dust:**

- ✅ **Уникальная идентификация:** Вероятность коллизии (два инвойса с одинаковым хвостом) < 0.0001%.
- ✅ **Точная проверка:** Не нужен "Fuzzy Matching" с погрешностью. Проверка: `value === expectedValue`.
- ✅ **Простота:** Не требует сложной логики обработки множественных совпадений.
- ✅ **Прозрачность:** Пользователь видит точную сумму и понимает механизм.

**Параметры:**

- **Polling interval:** 10 секунд (быстрое обновление статуса).
- **Dust Range:** 0.000001 - 0.000999 (для 6 decimals) или аналогично для других decimals.
- **Max Dust Value:** ~$0.01 (незначительная сумма для пользователя, но достаточная для уникальности).

---

## 4.4. Механика Донатов (Donation Mechanics)

Мы отказались от чекбокса "1% Developer Tip" в MVP в пользу пост-оплатного флоу.

### 4.4.1. Fast Lane (Widget)

- **Context:** Появляется сразу после успешной оплаты инвойса.
- **Currency:** **Native Currency Only** (ETH, MATIC, etc.).
- **Reasoning:** Исключает необходимость `approve` для ERC20 токенов. Это позволяет сделать донат в **один клик** (одна транзакция `sendTransaction`).
- **UX:** Кнопки с фиксированными суммами (Smart Tiers), конвертированными в нативный токен.

### 4.4.2. Slow Lane (Modal)

- **Context:** Вызывается через футер ("Support VoidPay").
- **Currency:** Любой токен (Native или ERC20).
- **Flow:** Стандартный Web3 флоу (Approve -> Transfer для ERC20).
- **Treasury:** Адрес получателя донатов (Dev Treasury) **захардкожен** в приложении.

---

_Конец Части 4_
